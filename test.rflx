package Test is

   type Enum is (
      TEST => 0
   ) with Size => 16;

   type A is mod 2**8;
   type T is array of A;

   type Test_Frame is
      message
         Test : T
            with Size => 16;
         B : Enum;
      end message;

end Test;